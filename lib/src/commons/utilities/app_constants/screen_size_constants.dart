import 'dart:ui';

const double originalSizeWidth = 360;
const double origitanlSizeHeight = 800;

const originalSize = Size(originalSizeWidth, origitanlSizeHeight);
const double appBarHeight = 40;
const double pageHorizontalPadding = 16;
const int imageDisplaySize = 200;
