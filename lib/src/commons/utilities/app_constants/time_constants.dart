// Delay để kịp hiển thị hiệu ứng lan toả các nút bấm
const nextPageTransitionDelayMilliseconds = 100;
const nextPageTransitionDelayDuration = Duration(milliseconds: 100);
const buttonTransitionDuration = Duration(milliseconds: 300);

// Delay để kịp hiển thị hiệu ứng lan toả
const oldDuration = Duration(days: 30);
